{
  if (size == 0) {
    return false;
  }
  if (innerMap == null) {
    for (int i=0; i < size; i++) {
      if (listValues[i].equals(value))       return true;
    }
    return false;
  }
 else {
    return innerMap.containsValue(value);
  }
}
